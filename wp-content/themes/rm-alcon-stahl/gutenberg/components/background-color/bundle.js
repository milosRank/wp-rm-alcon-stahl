// Import Background Color Component
import { BlockBackgroundColor } from "../../components/background-color";
import renderBackgroundOptions from "../../components/background-color";
import BACKGROUND_OPTIONS from "../../components/background-color/options";

export const BackgroundColorComponent = {
    BlockBackgroundColor: { BlockBackgroundColor },
    renderBackgroundOptions: renderBackgroundOptions,
    BACKGROUND_OPTIONS: BACKGROUND_OPTIONS
}